{
  "name": "OCaml and Haskell DevContainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {
    "editor.formatOnSave": true,
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  },
  "extensions": [
    "ocamllabs.ocaml-platform",
    "justusadam.language-haskell",
    "gitlens.gitlens"
  ],
  "forwardPorts": [],
  "postCreateCommand": "bash -c 'export PATH=\"$HOME/.ghcup/bin:$HOME/.cabal/bin:$HOME/.local/bin:$PATH\" && cabal update && cabal build || echo \"Initial build failed. Run cabal build manually.\"'",
  "remoteUser": "vscode"
}